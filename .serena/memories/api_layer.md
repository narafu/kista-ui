# API 계층

SSOT: `docs/agents/entities.md`(도메인 API·queryKey·DTO·KIS quirk) + `docs/agents/shared.md`(apiFetch/clientFetch·cache·proxy) + `docs/agents/app.md`(Route Handler·SSE). 이 메모리에 내용을 중복 기재하지 않는다.
